frontend/
  src/
    api/              # API client (axios, fetch wrappers)
    components/       # Reusable UI components
    features/
      auth/           # Login, register, JWT handling
      channels/       # Channel list, creation, sidebar
      chat/           # Chat view, threads, reactions
      presence/       # Online presence indicator
    hooks/            # Zustand/React Query hooks
    pages/            # Page-level components
    styles/           # Tailwind config, global styles
    utils/            # Helpers (date formatting, etc.)
  public/
  vite.config.js
  package.json
